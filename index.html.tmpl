## "Crandard" template
## robcranfill@gmail.com
##
## based on a template for WeeWX, which is
## Copyright 2009-2018 Tom Keffer
##

#errorCatcher Echo

##
## Specifying an encoding of UTF-8 is usually safe:
#encoding UTF-8
##

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="refresh" content="30">
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
    <meta http-equiv="Pragma" content="no-cache" />
    <meta http-equiv="Expires" content="0" />

    <link href='https://fonts.googleapis.com/css?family=Stylish' rel='stylesheet'>
    <link rel="icon" type="image/png" href="favicon.ico" />
    <style>
        .currentTemp {
            font-family: 'Stylish'; font-size: 160px;
            # (use this for debugging, here and/or in the following blocks)
            # border: 1px outset red;
            background-color: lightblue;
            text-align: center;
            line-height: 80%;
            padding-top:    15px;
            padding-bottom:  1px;
            }
        .tempSummary {
            font-family: 'Stylish'; font-size:  48px;
            background-color: lightblue;
            text-align: center;
            # line-height: 80%;
            padding-top:    1px;
            padding-bottom: 5px;
            }
        .currentWind {
            font-family: 'Stylish'; font-size: 96px;
            background-color: lightgreen;
            text-align: center;
            line-height: 80%;
            padding-top:    12px;
            padding-bottom: 12px;
            }
        .rainSummary {
            font-family: 'Stylish'; font-size:  48px;
            background-color: lightblue;
            text-align: center;
            # line-height: 100%;
            padding-top:    12px;
            padding-bottom: 12px;
            }
        .currentTime {
            font-family: 'Stylish'; font-size: 32px;
            background-color: lightgreen;
            text-align: center;
            # line-height: 100%;
            padding-top:    15px;
            padding-bottom: 15px;
            }
    </style>
</head>
<body>

  <div class="currentTemp">$current.outTemp.format(format_string="%.0f")</div>

  <div class="tempSummary">$span($hour_delta=24).outTemp.min.format(format_string="%.0f") &ndash; $span($hour_delta=24).outTemp.max.format(format_string="%.0f") (24 hr)</div>

   <div class="currentWind">$span($time_delta=300).windSpeed.max.format(add_label=False) &ndash; $span($time_delta=1800).windSpeed.max.format(add_label=False) MPH</div>

   <div class="rainSummary">Rain today: $day.rain.sum.format(add_label=False)&Prime;&semi; last 7 days: $span($day_delta=7).rain.sum.format(add_label=False)&Prime;</div>

   <div class="currentTime">$current.dateTime.format("%-I:%M %p - %A, %-d %b %Y")</div>

</body>
</html>
